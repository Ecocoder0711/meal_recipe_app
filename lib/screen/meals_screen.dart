import 'package:flutter/material.dart';

class Mealscreen extends StatelessWidget {
  const Mealscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Meal Screen", style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
